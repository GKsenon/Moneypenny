package com.gksenon.moneypenny.domain

import java.util.UUID

val BANK_ID = UUID.nameUUIDFromBytes("Bank".toByteArray()).toString()
val HOST_ID = UUID.nameUUIDFromBytes("HOST".toByteArray()).toString()
